enum ColorMode {
  light = "light",
  dark = "dark",
}

enum TaskStatus {
  toDo = "To Do",
  inProgress = "In Progress",
  done = "Done",
}

enum ToastStatus {
  success = "success",
  error = "error",
  loading = "loading",
}

export { ColorMode, TaskStatus, ToastStatus };
